# Privacy And Security

- Default HTTP probing is restricted to localhost addresses.
- Non-local targets require explicit allowlisting.
- Sensitive headers are read from environment variables and are not persisted.
- Reports may contain API response data, so users should review outputs before sharing.
- The fixture demo is deterministic and does not call external services.

